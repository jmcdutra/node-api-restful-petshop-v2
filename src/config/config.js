const config = {
    "mysql": {
        "database": "petshop",
        "user": "dutra",
        "password": "admin",
        "host": "localhost"
    },
    "server": {
        "port": 8000
    }
}

module.exports = config